.DS_Store
.build/
.swiftpm/
*.xcodeproj
xcuserdata/

# Cloned reference sources + the C oracle build — never part of the Swift build.
/References/

# Generated golden fixtures are committed; build artifacts and scratch WAVs are not.
/Scratch/
/Renders/
*.wav
!Tests/**/*.wav

# Copyrighted game assets + traces derived from them — never committed.
/Resources/
Tests/WestwoodADLTests/Fixtures/DUNE*.trace
