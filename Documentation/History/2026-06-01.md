# 2026-06-01

- Bootstrapped the SwiftOPL3 package: `Package.swift` (two libraries `SwiftOPL3` + `WestwoodADL`, Memoirs dep, test targets), `COPYING.LESSER` (LGPL-2.1), `CLAUDE.md`, `CurrentState.md`, `README.md`.
- Wrote the design docs: `Documentation/Plan.md`, `Architecture/{Overview,OPL3,WestwoodADL,Logging,Testing}.md`, `Formats/ADL.md`.
- Added the debug-only logging seam `Sources/SwiftOPL3/OPLLog.swift` (Memoirs behind `#if OPL_TRACE`, zero-cost in release) and compiling stubs `OPL3Chip.swift` / `ADLPlayer.swift` with the public API surface.
- Added skeleton smoke tests and the reference/golden generation script stubs `Scripts/{fetch-references,gen-chip-goldens,gen-adl-traces}.sh`.
- Decisions locked: chip ⇐ Nuked-OPL3, driver ⇐ AdPlug `CadlPlayer` (ScummVM/NScumm cross-ref), LGPL-2.1, pure Swift, Memoirs logging compiled out in release.
- Resolved the Memoirs dependency: real coordinates are `https://github.com/bealex/memoirs-ios.git`, product `Memoirs`, package identity `memoirs-ios`. Pinned to `2.0.0 ..< 2.1.0` because 2.1+ raised the platform floor to macOS 15 (we target macOS 13 / iOS 16); 2.0.x supports macOS 13. `Package.swift` updated; `swift build` (default + `-DOPL_TRACE`) and `swift test` now green.
- Cloned the reference oracles into `References/` (gitignored); pinned Nuked-OPL3 at commit `cfedb09efc03f1d7b5fc1f04dd449d77d8c49d50` (version 1.8, 2024-07-01) as the chip transcription source.
- Phase 1.1 — ported the constant tables verbatim from `opl3.c` into `Sources/SwiftOPL3/OPL3Tables.swift`: `logsinrom[256]` (opl3.c:75), `exprom[256]` (opl3.c:114), `mt[16]` (opl3.c:155), `kslrom[16]` (opl3.c:163), `kslshift[4]` (opl3.c:167), `egIncstep[4][4]` (opl3.c:175), `adSlot[0x20]` (opl3.c:186), `chSlot[18]` (opl3.c:191), with length + spot-value parity tests in `OPL3TablesTests.swift`.
- Phase 1.2 — types: `Slot`/`Channel`/`WriteBuf` structs + `SampleRef`/`TremRef`/`OPL3Const` in `OPL3Types.swift` (opl3.h:53/85/108). Adopted the **index-based pointer model**: Nuked's `int16_t *mod` / `*trem` / `*out[4]` / `slot->channel` / `channel->pair` become indices into the chip's `slot`/`channel` arrays, with `.zero` standing in for `&chip->zeromod`; resolved via `OPL3Chip.sample(at:)` / `tremValue(_:)`. Expanded `OPL3Chip` to hold all `opl3_chip` state and transcribed `OPL3_Reset` (opl3.c:1293) + `OPL3_ChannelSetupAlg` (opl3.c:848). Ported the exp converter + 8 log-sine waveforms `OPL3_EnvelopeCalcExp`/`Sin0..7` + `envelope_sin[8]` (opl3.c:211–366) in `OPL3Waveforms.swift`. Tests: `OPL3ResetTests`, `OPL3WaveformTests`.
- Phase 1.3–1.6 — register-write layer `OPL3Registers.swift` (`OPL3_SlotWrite20/40/60/80/E0`, `EnvelopeUpdateKSL`, `ChannelWriteA0/B0/C0`, `ChannelUpdateAlg`, `ChannelKeyOn/Off`, `EnvelopeKeyOn/Off`, `ChannelUpdateRhythm`, `ChannelSet4Op`, `OPL3_WriteReg` dispatch); envelope generator `OPL3Envelope.swift` (`OPL3_EnvelopeCalc`, opl3.c:387 — including the sign-extended `~eg_rout >> (4-shift)` attack increment); phase generator + slot output `OPL3Phase.swift` (`OPL3_PhaseGenerate`, `SlotGenerate`, `SlotCalcFB`, `ProcessSlot`, `ClipSample`). Tests: `OPL3RegisterTests`, `OPL3EnvelopeTests`, `OPL3PhaseTests`.
- Phase 1.7 — top level `OPL3Generate.swift`: `OPL3_Generate4Ch` (opl3.c:1111, with the default `OPL_QUIRK_CHANNELSAMPLEDELAY` slot-group split + one-sample right-channel delay), `Generate4ChResampled` (opl3.c:1263), `OPL3_WriteRegBuffered` (opl3.c:1472); wired public `generate`/`generateResampled`/`generateStream`/`writeBuffered`. Tests: `OPL3GenerateTests` (idle silence, audible note, sample-delay quirk, determinism, resampler, stream, buffered queue).
- **Phase 2 (chip golden) bootstrapped + GREEN.** `Scripts/chip_golden_harness.c` compiles the cloned Nuked-OPL3 and dumps PCM for a fixed sine-note register script; `Scripts/gen-chip-goldens.sh` builds/runs it → `Tests/SwiftOPL3Tests/Fixtures/sine_note_49716.pcm`. `OPL3GoldenTests` renders the same script through `OPL3Chip` and asserts **bit-exact (sample-for-sample) PCM equality** — 4096 stereo samples match Nuked exactly. The whole Phase 1 chip port is verified bit-identical, not by eye.
- Full suite (after Phase 1): 53 tests green; zero warnings on clean build in both default and `-DOPL_TRACE` configs.
- **Phase 2 (chip golden) completed.** Parameterised `Scripts/chip_golden_harness.c` (argv-selected script) + `gen-chip-goldens.sh` to emit five fixtures; `OPL3GoldenTests` is now a parameterised test asserting **bit-exact PCM** for: `sine`, all 8 `waveforms`, an OPL3 4-op patch (`fourop`), `rhythm` mode (BD/SD/TT/CY/HH — exercises the `rm_*` phase quirks), and the 49716→44100 `resample` path. All five match Nuked sample-for-sample.
- **Phase 3 — ADL v2 parser.** `Sources/WestwoodADL/ADLData.swift` transcribes `CadlPlayer::load` (adl.cpp:2767) — version detection, soundData extraction, the 0xFF trackEntries tail, the bad-offset rejections — plus `getProgram`/`getInstrument` (adl.cpp:286/308). AdPlug reports Dune II v2 as `_version=3` (250 programs). Tested with a synthetic v2/v3 file in `ADLDataTests` (version, subsong count, offsets, corrupt-data rejection).
- **Phase 4 — Westwood ADL driver.** `AdLibDriver.swift` + `AdLibDriver+Opcodes.swift`: full transcription of AdPlug `AdLibDriver` (adl.cpp, ScummVM/Kyra port) — `Channel` state, the 72 Hz `callback`/`setupPrograms`/`executePrograms` loop, `setupNote`/`setupInstrument`/`noteOn`/`noteOff`/`adjustVolume`/`calculateOpLevel1-2`, the three effects (`primaryEffectSlide`/`primaryEffectVibrato`/`secondaryEffect1`), **all 55 `update_*` opcodes**, the 75-entry dispatch table, and the ROM tables (`_regOffset`/`_freqTable`/`_unkTable2*`/`_pitchBendTables`, incl. the verbatim `0x6F` quirk). Pointer model: `const uint8 *dataptr` → `Int?` index; effect function-pointers → enums; `writeOPL` → `OPLRegisterSink` + `OPLLog.reg` tap. `ADLPlayer.swift` transcribes `CadlPlayer` (load/rewind/update/play) and wires the driver to an `OPL3Chip` via `ChipSink`. Tests: `AdLibDriverTests` (tables + the seeded `getRandomNr` PRNG).
- **Phase 4 driver golden (trace equivalence) — GREEN.** `gen-adl-traces.sh` builds a harness that `#include`s the cloned `adl.cpp` (heavy AdPlug headers stubbed) and drives its `AdLibDriver` through a recording `Copl` over a synthetic sound block → `Fixtures/synth_track.{trace,bin}`. `ADLTraceTests` drives the *ported* driver with the identical sequence over the same bytes and asserts the `(reg,val)` stream matches AdPlug **index-for-index** (56 writes covering reset, stopAllChannels, setupInstrument, key-on note, pitchBend, and the slide effect across ticks).
- **Phase 5 (end-to-end) — first light.** `ADLPlayerTests` builds a full synthetic `.ADL`, loads it through the public `ADLPlayer`, ticks `update()` at 72 Hz driving an `OPL3Chip`, and confirms a keyed note renders **audible, deterministic** PCM. (A real `DUNE*.ADL` listen still pends the asset.)
- Full suite (after synthetic Phase 4/5): 66 tests green across 14 suites.
- **Real Dune II music verified (the acceptance test).** User supplied `Resources/Music/DUNE*.ADL` (21 tracks). Built a second AdPlug oracle harness (`gen-adl-traces.sh` real-track section) using a `fopen`-backed `binistream` so AdPlug's *real* `CadlPlayer::load → rewind → update` runs on an actual file. `ADLSongTraceTests` loads `DUNE8.ADL` subsong 2 through our `ADLPlayer` and asserts the writeOPL stream matches AdPlug **index-for-index over 1178 writes** of an audible melody (key-ons, instrument loads, volume envelopes) — full-stack driver parity on real game data.
- **Phase 5 listen tool.** Added the `adlrender` example target (`Sources/adlrender/main.swift`): loads a `.ADL`, drives `ADLPlayer`→`OPL3Chip` at 72 Hz, writes a 44.1 kHz stereo WAV. Rendered `Renders/DUNE8_sub2.wav` and `Renders/DUNE1_sub2.wav` (45 s each, peak ≈ 5710/4571). `ADLPlayerTests.realTrackAudible` asserts DUNE8 sub 2 renders peak > 1000 through the public API.
- Full suite: **68 tests green** across 15 suites; zero warnings (default + `-DOPL_TRACE`). `.gitignore` updated to exclude the game assets (`/Resources/`), rendered WAVs (`/Renders/`), and `DUNE*.trace` (derived from copyrighted data).
